import 'package:flutter_modular/flutter_modular.dart';

/// Root module — didaftarkan saat boot.
/// Module fitur (inspections, programs, dll) akan ditambahkan di sini.
class AppModule extends Module {
  @override
  void binds(i) {
    // Shared dependencies akan ditambahkan di sini
  }

  @override
  void routes(r) {
    // Module routes akan ditambahkan di sini
  }
}
